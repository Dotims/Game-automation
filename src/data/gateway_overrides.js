// src/data/gateway_overrides.js

/**
 * MAPPING:
 * "Current Map Name": {
 *    "Target Map Name": { x: 12, y: 34 }
 * }
 */

const GATEWAY_OVERRIDES = {
    "Skalne Cmentarzysko p.4": {
        "Skalne Cmentarzysko p.5": { x: 5, y: 10 }
    }
};

module.exports = GATEWAY_OVERRIDES;
